{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "OwlMonitor (macOS)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/target/owl_monitor",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${userHome}",
            "MIMode": "lldb",
            "preLaunchTask": "build with CMake"
        }
    ]
}
